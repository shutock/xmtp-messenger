import { Connect } from "@/modules";

import styles from "./page.module.scss";

const Page = () => {
  return (
    <main>
      <h1 className={styles.title}>
        first of all tou need to connect your wallet
      </h1>
    </main>
  );
};

export default Page;
